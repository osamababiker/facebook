import 'package:flutter/material.dart';

// Colors that we use in our app
const kPrimaryColor = Colors.blue;
var kSecondaryColor = Colors.grey.withOpacity(0.09);

const double kDefaultPadding = 20.0;
